import { describe, it } from "vitest";

describe("ingestion", () => {
  it.todo("parses valid CSV");
  it.todo("fails fast on missing required columns");
  it.todo("skips invalid rows with warnings");
  it.todo("normalizes and builds transactions");
});
